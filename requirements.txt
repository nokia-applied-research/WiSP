# Pinned to the stack the paper's numbers were produced on.
# Install torch via the index that matches your CUDA before this file,
# e.g.:  pip install torch --index-url https://download.pytorch.org/whl/cu121
vllm==0.11.2
numpy>=1.24
matplotlib>=3.7
datasets>=2.14
